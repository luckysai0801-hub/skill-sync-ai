# Example environment variables for SkillSync backend
# Copy this file to ".env" and edit as needed. The application uses python-dotenv
# (if installed) to load variables from .env when running locally.

# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/skillsync
